import React from 'react';

const Title = props => (
      <div>
        <h1>Weather Search</h1>
        <p className="subhead">Get current weather conditions</p>
      </div>
  );


export default Title;